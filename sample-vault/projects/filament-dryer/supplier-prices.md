---
public: false
title: Supplier prices and contacts
---

This note sits inside a published project and must never appear on the site. It exists so the
opt-out has a fixture: `public: false` on a note inside a project whose `index.md` says
`public: true`.

If the string `RIVERBEND-4417` ever shows up in the generated bundle, the filter is broken.
