---
public: true
title: Loose idea with no project around it
---

This folder has no `index.md`, so it is not a project and nothing in it is read — including this
note, which says `public: true` precisely to prove that the key does not rescue a folder that was
never a project in the first place.

If the string `UNANCHORED-2931` shows up in the generated bundle, discovery is broken.
